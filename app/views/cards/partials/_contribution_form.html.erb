<%= simple_form_for [@card, @contribution] do |f| %>
  <%= f.input :contributor_name, label: 'Your name please' %>
  <%= f.input :content, as: :text, label: 'Write a nice message' %>
  <%= f.button :submit%>
<% end %>